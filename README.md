# ATM10C
custom ATM10 based pack

use these launch options `-XX:+UseG1GC -XX:MaxGCPauseMillis=50` and use 12-16GB of RAM and USE JAVA 21, not 26 that most linux distros come with

and disable the watchdog for the server
